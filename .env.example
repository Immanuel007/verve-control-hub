# Optional: override the API base URL.
# By default, the app routes through the Lovable Cloud edge function proxy
# at ${VITE_SUPABASE_URL}/functions/v1/obelix-proxy which forwards to the
# internal Quickteller host. Set this only if you have direct access.
# VITE_API_BASE_URL=http://quickteller-merchant-ui.test.kube.iswke
